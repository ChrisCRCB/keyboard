import 'keyboard_controller.dart';

/// The possible modes for a [KeyboardController].
enum KeyboardMode {
  /// Normal operation.
  normal,

  /// Navigation.
  navigation,

  /// Numbers.
  numbers,
}
